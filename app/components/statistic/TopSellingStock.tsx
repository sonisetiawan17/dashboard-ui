'use client';

import Table from '../Table';
import Card from '../card/Card';

const TopSellingStock = () => {
  return (
    <Card title="Top Selling Stock" link table left>
      <Table />
    </Card>
  );
};

export default TopSellingStock;
